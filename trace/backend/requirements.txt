# Pinned floor versions; the authoritative list lives in pyproject.toml.
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
sqlalchemy[asyncio]>=2.0.36
aiosqlite>=0.20
python-multipart>=0.0.12
minio>=7.2
clickhouse-connect>=0.8
httpx>=0.27
cryptography>=43
# Optional anchor backends. Present in the image so `opentimestamps` and `evm`
# are selectable without a rebuild; neither is used unless configured.
opentimestamps>=0.4
eth-account>=0.13
